# js-escrevenome
function setup() {
  createCanvas(600, 600);
  backgtound("back");
}

function draw() {
  stroke("blue");
  fill("red");
  if(mouseispressed){
    rect(mouseX, mouseY, 20,35);
    
}
  
}
